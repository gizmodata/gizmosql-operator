{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: gizmosql-operator
    name: gizmosql-operator-gizmosqlserver-admin-role
rules:
    - apiGroups:
        - gizmodata.com
      resources:
        - gizmosqlservers
      verbs:
        - '*'
    - apiGroups:
        - gizmodata.com
      resources:
        - gizmosqlservers/status
      verbs:
        - get
{{- end }}
